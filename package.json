{
  "name": "ghost-pro-academy",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "shared"
  ],
  "scripts": {
    "build:shared": "npm run build --workspace=shared",
    "start:backend": "build:shared && npm run start --workspace=backend",
    "start:frontend": "build:shared && npm run start --workspace=frontend",
    "build:backend": "build:shared && npm run build --workspace=backend",
    "build:frontend": "build:shared && npm run build --workspace=frontend",
    "test:backend": "npm run test --workspace=backend",
    "test:frontend": "npm run test --workspace=frontend"
  }
}
